# Movie-Recommendation-system-
This project is a content-based movie recommendation system. It suggests similar movies based on genre and keywords of the movie entered by the user. The system uses TF(how often a word appears in one document)-IDF (how rare that word is across all documents)and cosine similarity to find and recommend movies with similar content
